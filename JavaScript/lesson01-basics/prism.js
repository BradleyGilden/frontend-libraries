#!/usr/bin/env node

//practice program calulates volume of a prism

a_base = window.prompt("area of base")
height = window.prompt("Enter Height")

console.log("volume of shape:", Number(a_base) * Number(height))
